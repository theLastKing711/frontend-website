import { Paper, styled } from "@mui/material";

export const StyledMain = styled('main')( ({theme}) => ({
}));



export const StyledProductDetailsCardContainer = styled(Paper)( ({theme}) => ({
    padding: '7.56rem 0 8.19rem',
}))